#include <cmath>

/*
    Maybe it wanted me to use some math formula to compute sqrt,
    but I just used a library function
*/

class Solution {
public:
    int mySqrt(int x) {
        return sqrt(x);
    }
};

int main()
{

}